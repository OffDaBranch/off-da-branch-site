# Skill Evals And Test Prompts

BranchOps Codex Agency Operating System
Version: v4.2 Unified Final Max - Skill Packs + Cannabis / Hemp / Regulated Markets
Master Source: OffDaBranchHoldings/Branch-Off-Holdings
Rule: Do not manually edit local repo copies except when syncing from master.

## Eval Rule
Every skill pack has at least one prompt. High-risk skills have normal, edge-case, and failure/escalation prompts. Each prompt tests department routing, role card selection, skill-pack selection, permission boundaries, reviewer chain, validation, source-of-truth update, decision, and founder gate when required.

## 1. `branchops-role-routing`
1. Test branchops-role-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 2. `branchops-executive-review`
1. Test branchops-executive-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 3. `branchops-founder-decision-gate`
1. Test branchops-founder-decision-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 4. `branchops-handoff-generation`
1. Test branchops-handoff-generation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 5. `branchops-source-of-truth-update-check`
1. Test branchops-source-of-truth-update-check: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 6. `branchops-risk-classification`
1. Test branchops-risk-classification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 7. `branchops-operating-pack-sync`
1. Test branchops-operating-pack-sync: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 8. `branchops-repo-doc-sync`
1. Test branchops-repo-doc-sync: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 9. `branchops-agents-md-review`
1. Test branchops-agents-md-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 10. `branchops-decision-log-update`
1. Test branchops-decision-log-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 11. `branchops-github-repo-review`
1. Test branchops-github-repo-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 12. `branchops-pr-review`
1. Test branchops-pr-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 13. `branchops-issue-triage`
1. Test branchops-issue-triage: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 14. `branchops-commit-review`
1. Test branchops-commit-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 15. `branchops-release-readiness-review`
1. Test branchops-release-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 16. `branchops-changelog-builder`
1. Test branchops-changelog-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 17. `branchops-readme-updater`
1. Test branchops-readme-updater: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 18. `branchops-docs-consistency-check`
1. Test branchops-docs-consistency-check: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 19. `branchops-repo-structure-audit`
1. Test branchops-repo-structure-audit: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 20. `branchops-dependency-review`
1. Test branchops-dependency-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 21. `branchops-ci-debugging`
1. Test branchops-ci-debugging: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 22. `branchops-github-actions-workflow-build`
1. Test branchops-github-actions-workflow-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 23. `branchops-github-issue-template-build`
1. Test branchops-github-issue-template-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 24. `branchops-pr-template-build`
1. Test branchops-pr-template-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 25. `branchops-code-owner-routing`
1. Test branchops-code-owner-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 26. `branchops-cloudflare-worker-build`
1. Test branchops-cloudflare-worker-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 27. `branchops-cloudflare-worker-debug`
1. Test branchops-cloudflare-worker-debug: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 28. `branchops-wrangler-config-review`
1. Test branchops-wrangler-config-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 29. `branchops-d1-schema-change`
1. Test branchops-d1-schema-change: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 30. `branchops-d1-migration-review`
1. Test branchops-d1-migration-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 31. `branchops-r2-file-upload-feature`
1. Test branchops-r2-file-upload-feature: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 32. `branchops-kv-durable-objects-review`
1. Test branchops-kv-durable-objects-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 33. `branchops-queues-workflows-build`
1. Test branchops-queues-workflows-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 34. `branchops-vectorize-retrieval-build`
1. Test branchops-vectorize-retrieval-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 35. `branchops-ai-gateway-routing`
1. Test branchops-ai-gateway-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ai-gateway-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ai-gateway-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 36. `branchops-cloudflare-access-auth-review`
1. Test branchops-cloudflare-access-auth-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 37. `branchops-observability-logging-review`
1. Test branchops-observability-logging-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 38. `branchops-deployment-risk-review`
1. Test branchops-deployment-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-deployment-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-deployment-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 39. `branchops-preview-deploy-review`
1. Test branchops-preview-deploy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-preview-deploy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-preview-deploy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 40. `branchops-production-deploy-gate`
1. Test branchops-production-deploy-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-production-deploy-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-production-deploy-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 41. `branchops-ai-platform-build`
1. Test branchops-ai-platform-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ai-platform-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ai-platform-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 42. `branchops-agent-workflow-design`
1. Test branchops-agent-workflow-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 43. `branchops-agent-memory-design`
1. Test branchops-agent-memory-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-agent-memory-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-agent-memory-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 44. `branchops-rag-knowledge-index-build`
1. Test branchops-rag-knowledge-index-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 45. `branchops-file-aware-chat-build`
1. Test branchops-file-aware-chat-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 46. `branchops-model-routing-review`
1. Test branchops-model-routing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 47. `branchops-prompt-system-design`
1. Test branchops-prompt-system-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 48. `branchops-ai-evals-design`
1. Test branchops-ai-evals-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ai-evals-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ai-evals-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 49. `branchops-guardrail-review`
1. Test branchops-guardrail-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 50. `branchops-ai-safety-risk-review`
1. Test branchops-ai-safety-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ai-safety-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ai-safety-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 51. `branchops-chat-ui-feature-build`
1. Test branchops-chat-ui-feature-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 52. `branchops-admin-ai-dashboard-build`
1. Test branchops-admin-ai-dashboard-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-admin-ai-dashboard-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-admin-ai-dashboard-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 53. `branchops-ai-cost-control-review`
1. Test branchops-ai-cost-control-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ai-cost-control-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ai-cost-control-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 54. `branchops-ai-usage-logging-review`
1. Test branchops-ai-usage-logging-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ai-usage-logging-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ai-usage-logging-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 55. `branchops-ai-productization-review`
1. Test branchops-ai-productization-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ai-productization-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ai-productization-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 56. `branchops-backend-api-build`
1. Test branchops-backend-api-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 57. `branchops-frontend-ui-build`
1. Test branchops-frontend-ui-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 58. `branchops-full-stack-feature-slice`
1. Test branchops-full-stack-feature-slice: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 59. `branchops-auth-feature-build`
1. Test branchops-auth-feature-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 60. `branchops-api-integration-build`
1. Test branchops-api-integration-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 61. `branchops-database-schema-review`
1. Test branchops-database-schema-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 62. `branchops-mobile-ios-feature-build`
1. Test branchops-mobile-ios-feature-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 63. `branchops-expo-react-native-build`
1. Test branchops-expo-react-native-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 64. `branchops-typescript-refactor`
1. Test branchops-typescript-refactor: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 65. `branchops-test-suite-builder`
1. Test branchops-test-suite-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 66. `branchops-error-handling-review`
1. Test branchops-error-handling-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 67. `branchops-rate-limit-abuse-control`
1. Test branchops-rate-limit-abuse-control: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 68. `branchops-admin-dashboard-build`
1. Test branchops-admin-dashboard-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 69. `branchops-user-settings-feature-build`
1. Test branchops-user-settings-feature-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 70. `branchops-saas-mvp-slice-builder`
1. Test branchops-saas-mvp-slice-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 71. `branchops-website-intake-build`
1. Test branchops-website-intake-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 72. `branchops-landing-page-build`
1. Test branchops-landing-page-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 73. `branchops-website-copy-review`
1. Test branchops-website-copy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 74. `branchops-seo-metadata-review`
1. Test branchops-seo-metadata-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 75. `branchops-lead-routing-build`
1. Test branchops-lead-routing-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 76. `branchops-contact-form-privacy-review`
1. Test branchops-contact-form-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-contact-form-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-contact-form-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 77. `branchops-public-trust-page-review`
1. Test branchops-public-trust-page-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-public-trust-page-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-public-trust-page-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 78. `branchops-offer-page-build`
1. Test branchops-offer-page-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 79. `branchops-licensing-funnel-build`
1. Test branchops-licensing-funnel-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-licensing-funnel-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-licensing-funnel-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 80. `branchops-website-qa-check`
1. Test branchops-website-qa-check: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 81. `branchops-airtable-source-of-truth-sync`
1. Test branchops-airtable-source-of-truth-sync: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 82. `branchops-airtable-schema-design`
1. Test branchops-airtable-schema-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 83. `branchops-airtable-record-creation`
1. Test branchops-airtable-record-creation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 84. `branchops-airtable-automation-spec`
1. Test branchops-airtable-automation-spec: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 85. `branchops-notion-sop-builder`
1. Test branchops-notion-sop-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 86. `branchops-notion-database-design`
1. Test branchops-notion-database-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 87. `branchops-crm-pipeline-design`
1. Test branchops-crm-pipeline-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 88. `branchops-data-quality-review`
1. Test branchops-data-quality-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 89. `branchops-deduplication-review`
1. Test branchops-deduplication-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 90. `branchops-reporting-dashboard-spec`
1. Test branchops-reporting-dashboard-spec: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 91. `branchops-daily-work-ledger-update`
1. Test branchops-daily-work-ledger-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 92. `branchops-build-queue-prioritization`
1. Test branchops-build-queue-prioritization: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 93. `branchops-validation-log-update`
1. Test branchops-validation-log-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 94. `branchops-source-map-update`
1. Test branchops-source-map-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 95. `branchops-operating-record-builder`
1. Test branchops-operating-record-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 96. `branchops-holdco-governance-doc-control`
1. Test branchops-holdco-governance-doc-control: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-holdco-governance-doc-control: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-holdco-governance-doc-control: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 97. `branchops-operating-agreement-review`
1. Test branchops-operating-agreement-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 98. `branchops-corporate-record-book-update`
1. Test branchops-corporate-record-book-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 99. `branchops-written-consent-builder`
1. Test branchops-written-consent-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 100. `branchops-entity-separation-review`
1. Test branchops-entity-separation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 101. `branchops-compliance-calendar-build`
1. Test branchops-compliance-calendar-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 102. `branchops-trust-activation-planning`
1. Test branchops-trust-activation-planning: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-trust-activation-planning: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-trust-activation-planning: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 103. `branchops-digital-vault-setup-spec`
1. Test branchops-digital-vault-setup-spec: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 104. `branchops-beneficial-ownership-file-review`
1. Test branchops-beneficial-ownership-file-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-beneficial-ownership-file-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-beneficial-ownership-file-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 105. `branchops-registered-agent-compliance-review`
1. Test branchops-registered-agent-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 106. `branchops-document-retention-policy-build`
1. Test branchops-document-retention-policy-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 107. `branchops-risk-register-update`
1. Test branchops-risk-register-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 108. `branchops-external-counsel-handoff`
1. Test branchops-external-counsel-handoff: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 109. `branchops-contract-intake-review`
1. Test branchops-contract-intake-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-contract-intake-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-contract-intake-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 110. `branchops-governance-audit-readiness-review`
1. Test branchops-governance-audit-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-governance-audit-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-governance-audit-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 111. `branchops-treasury-setup-review`
1. Test branchops-treasury-setup-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 112. `branchops-bank-account-readiness-check`
1. Test branchops-bank-account-readiness-check: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-bank-account-readiness-check: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-bank-account-readiness-check: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 113. `branchops-chart-of-accounts-build`
1. Test branchops-chart-of-accounts-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 114. `branchops-bookkeeping-workflow-design`
1. Test branchops-bookkeeping-workflow-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 115. `branchops-intercompany-controls-review`
1. Test branchops-intercompany-controls-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-intercompany-controls-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-intercompany-controls-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 116. `branchops-management-fee-review`
1. Test branchops-management-fee-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 117. `branchops-ip-royalty-flow-review`
1. Test branchops-ip-royalty-flow-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ip-royalty-flow-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ip-royalty-flow-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 118. `branchops-asset-lease-flow-review`
1. Test branchops-asset-lease-flow-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 119. `branchops-revenue-event-tracking`
1. Test branchops-revenue-event-tracking: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 120. `branchops-pricing-model-review`
1. Test branchops-pricing-model-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 121. `branchops-stripe-billing-review`
1. Test branchops-stripe-billing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-stripe-billing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-stripe-billing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 122. `branchops-cash-flow-forecast`
1. Test branchops-cash-flow-forecast: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cash-flow-forecast: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cash-flow-forecast: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 123. `branchops-monthly-close-checklist`
1. Test branchops-monthly-close-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 124. `branchops-cpa-handoff-package`
1. Test branchops-cpa-handoff-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 125. `branchops-finance-risk-escalation`
1. Test branchops-finance-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-finance-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-finance-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 126. `branchops-licensing-package-builder`
1. Test branchops-licensing-package-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-licensing-package-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-licensing-package-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 127. `branchops-ip-ownership-review`
1. Test branchops-ip-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ip-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ip-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 128. `branchops-trademark-readiness-review`
1. Test branchops-trademark-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 129. `branchops-copyright-readiness-review`
1. Test branchops-copyright-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 130. `branchops-brand-usage-review`
1. Test branchops-brand-usage-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 131. `branchops-royalty-model-design`
1. Test branchops-royalty-model-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 132. `branchops-contract-package-outline`
1. Test branchops-contract-package-outline: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-contract-package-outline: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-contract-package-outline: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 133. `branchops-domain-portfolio-review`
1. Test branchops-domain-portfolio-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 134. `branchops-domain-lease-flip-keep-classifier`
1. Test branchops-domain-lease-flip-keep-classifier: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-domain-lease-flip-keep-classifier: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-domain-lease-flip-keep-classifier: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 135. `branchops-web3-domain-review`
1. Test branchops-web3-domain-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 136. `branchops-digital-asset-vault-map`
1. Test branchops-digital-asset-vault-map: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 137. `branchops-brand-system-documentation`
1. Test branchops-brand-system-documentation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 138. `branchops-ip-risk-escalation`
1. Test branchops-ip-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-ip-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-ip-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 139. `branchops-licensee-onboarding-workflow`
1. Test branchops-licensee-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-licensee-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-licensee-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 140. `branchops-licensing-renewal-tracker`
1. Test branchops-licensing-renewal-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-licensing-renewal-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-licensing-renewal-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 141. `branchops-game-feature-build`
1. Test branchops-game-feature-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 142. `branchops-game-design-doc-builder`
1. Test branchops-game-design-doc-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 143. `branchops-gameplay-loop-review`
1. Test branchops-gameplay-loop-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 144. `branchops-game-ui-ux-review`
1. Test branchops-game-ui-ux-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 145. `branchops-game-asset-pipeline-review`
1. Test branchops-game-asset-pipeline-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 146. `branchops-digital-wearables-pipeline`
1. Test branchops-digital-wearables-pipeline: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 147. `branchops-game-economy-design`
1. Test branchops-game-economy-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 148. `branchops-in-game-store-review`
1. Test branchops-in-game-store-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 149. `branchops-avatar-customization-review`
1. Test branchops-avatar-customization-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 150. `branchops-game-qa-playtest`
1. Test branchops-game-qa-playtest: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 151. `branchops-third-party-game-asset-ip-review`
1. Test branchops-third-party-game-asset-ip-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-third-party-game-asset-ip-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-third-party-game-asset-ip-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 152. `branchops-game-release-gate`
1. Test branchops-game-release-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 153. `branchops-trapcotton-game-integration`
1. Test branchops-trapcotton-game-integration: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 154. `branchops-game-monetization-review`
1. Test branchops-game-monetization-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 155. `branchops-game-licensing-package`
1. Test branchops-game-licensing-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-game-licensing-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-game-licensing-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 156. `branchops-apparel-product-system`
1. Test branchops-apparel-product-system: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 157. `branchops-capsule-collection-planner`
1. Test branchops-capsule-collection-planner: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 158. `branchops-sku-size-system-builder`
1. Test branchops-sku-size-system-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 159. `branchops-printify-product-setup`
1. Test branchops-printify-product-setup: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 160. `branchops-shopify-product-page-review`
1. Test branchops-shopify-product-page-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 161. `branchops-merch-margin-review`
1. Test branchops-merch-margin-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 162. `branchops-fulfillment-workflow-review`
1. Test branchops-fulfillment-workflow-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 163. `branchops-brand-compliance-review`
1. Test branchops-brand-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 164. `branchops-product-launch-checklist`
1. Test branchops-product-launch-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 165. `branchops-vendor-sourcing-review`
1. Test branchops-vendor-sourcing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 166. `branchops-curriculum-build`
1. Test branchops-curriculum-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 167. `branchops-lesson-plan-builder`
1. Test branchops-lesson-plan-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 168. `branchops-assessment-builder`
1. Test branchops-assessment-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 169. `branchops-certification-pathway-build`
1. Test branchops-certification-pathway-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 170. `branchops-workforce-program-design`
1. Test branchops-workforce-program-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 171. `branchops-training-sop-builder`
1. Test branchops-training-sop-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 172. `branchops-learner-outcome-rubric`
1. Test branchops-learner-outcome-rubric: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 173. `branchops-education-compliance-review`
1. Test branchops-education-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 174. `branchops-school-partnership-package`
1. Test branchops-school-partnership-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-school-partnership-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-school-partnership-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 175. `branchops-curriculum-licensing-package`
1. Test branchops-curriculum-licensing-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-curriculum-licensing-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-curriculum-licensing-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 176. `branchops-grant-opportunity-review`
1. Test branchops-grant-opportunity-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-grant-opportunity-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-grant-opportunity-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 177. `branchops-grant-eligibility-review`
1. Test branchops-grant-eligibility-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-grant-eligibility-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-grant-eligibility-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 178. `branchops-grant-document-checklist`
1. Test branchops-grant-document-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-grant-document-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-grant-document-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 179. `branchops-budget-narrative-builder`
1. Test branchops-budget-narrative-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 180. `branchops-public-sector-bid-review`
1. Test branchops-public-sector-bid-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-public-sector-bid-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-public-sector-bid-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 181. `branchops-municipal-contract-readiness`
1. Test branchops-municipal-contract-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-municipal-contract-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-municipal-contract-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 182. `branchops-nj-public-works-readiness`
1. Test branchops-nj-public-works-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 183. `branchops-proposal-package-builder`
1. Test branchops-proposal-package-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 184. `branchops-compliance-requirements-review`
1. Test branchops-compliance-requirements-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 185. `branchops-submission-readiness-review`
1. Test branchops-submission-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 186. `branchops-prospect-list-builder`
1. Test branchops-prospect-list-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 187. `branchops-partnership-outreach-builder`
1. Test branchops-partnership-outreach-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-partnership-outreach-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-partnership-outreach-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 188. `branchops-sales-script-builder`
1. Test branchops-sales-script-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 189. `branchops-proposal-builder`
1. Test branchops-proposal-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 190. `branchops-mailchimp-campaign-builder`
1. Test branchops-mailchimp-campaign-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 191. `branchops-content-calendar-builder`
1. Test branchops-content-calendar-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 192. `branchops-social-distribution-plan`
1. Test branchops-social-distribution-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 193. `branchops-audience-ownership-review`
1. Test branchops-audience-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-audience-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-audience-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 194. `branchops-offer-positioning-review`
1. Test branchops-offer-positioning-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 195. `branchops-conversion-report-review`
1. Test branchops-conversion-report-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 196. `branchops-vendor-procurement-review`
1. Test branchops-vendor-procurement-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 197. `branchops-software-subscription-review`
1. Test branchops-software-subscription-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 198. `branchops-asset-inventory-system`
1. Test branchops-asset-inventory-system: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 199. `branchops-hiring-role-description-builder`
1. Test branchops-hiring-role-description-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 200. `branchops-contractor-onboarding-workflow`
1. Test branchops-contractor-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-contractor-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-contractor-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 201. `branchops-access-provisioning-review`
1. Test branchops-access-provisioning-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 202. `branchops-customer-support-workflow`
1. Test branchops-customer-support-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 203. `branchops-client-onboarding-workflow`
1. Test branchops-client-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 204. `branchops-feedback-triage-workflow`
1. Test branchops-feedback-triage-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 205. `branchops-kpi-dashboard-builder`
1. Test branchops-kpi-dashboard-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 206. `branchops-weekly-executive-report`
1. Test branchops-weekly-executive-report: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 207. `branchops-asset-performance-review`
1. Test branchops-asset-performance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 208. `branchops-automation-rate-report`
1. Test branchops-automation-rate-report: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 209. `branchops-blocker-resolution-workflow`
1. Test branchops-blocker-resolution-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 210. `branchops-operations-review-cadence`
1. Test branchops-operations-review-cadence: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 211. `branchops-property-services-estimate-system`
1. Test branchops-property-services-estimate-system: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 212. `branchops-lawn-landscaping-ops-system`
1. Test branchops-lawn-landscaping-ops-system: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 213. `branchops-lot-cleaning-ops-system`
1. Test branchops-lot-cleaning-ops-system: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 214. `branchops-snow-ice-ops-system`
1. Test branchops-snow-ice-ops-system: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 215. `branchops-equipment-tracker-review`
1. Test branchops-equipment-tracker-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 216. `branchops-safety-checklist-builder`
1. Test branchops-safety-checklist-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 217. `branchops-agriculture-rd-concept-review`
1. Test branchops-agriculture-rd-concept-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 218. `branchops-infusible-base-library-update`
1. Test branchops-infusible-base-library-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 219. `branchops-food-sku-development`
1. Test branchops-food-sku-development: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 220. `branchops-product-safety-review`
1. Test branchops-product-safety-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 221. `branchops-white-label-product-package`
1. Test branchops-white-label-product-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-white-label-product-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-white-label-product-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 222. `branchops-media-campaign-builder`
1. Test branchops-media-campaign-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 223. `branchops-script-story-builder`
1. Test branchops-script-story-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 224. `branchops-video-production-plan`
1. Test branchops-video-production-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-video-production-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-video-production-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 225. `branchops-rights-clearance-review`
1. Test branchops-rights-clearance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 226. `branchops-content-library-system`
1. Test branchops-content-library-system: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required.

## 227. `branchops-cannabis-regulated-market-routing`
1. Test branchops-cannabis-regulated-market-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-regulated-market-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-regulated-market-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 228. `branchops-cannabis-license-readiness-review`
1. Test branchops-cannabis-license-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-license-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-license-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 229. `branchops-cannabis-regulated-activity-classifier`
1. Test branchops-cannabis-regulated-activity-classifier: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-regulated-activity-classifier: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-regulated-activity-classifier: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 230. `branchops-cannabis-compliance-calendar-build`
1. Test branchops-cannabis-compliance-calendar-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-compliance-calendar-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-compliance-calendar-build: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 231. `branchops-cannabis-risk-register-update`
1. Test branchops-cannabis-risk-register-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-risk-register-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-risk-register-update: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 232. `branchops-cannabis-founder-decision-gate`
1. Test branchops-cannabis-founder-decision-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-founder-decision-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-founder-decision-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 233. `branchops-cannabis-external-counsel-handoff`
1. Test branchops-cannabis-external-counsel-handoff: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-external-counsel-handoff: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-external-counsel-handoff: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 234. `branchops-cannabis-jurisdiction-checklist`
1. Test branchops-cannabis-jurisdiction-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-jurisdiction-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-jurisdiction-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 235. `branchops-cannabis-state-rule-change-monitor`
1. Test branchops-cannabis-state-rule-change-monitor: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-state-rule-change-monitor: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-state-rule-change-monitor: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 236. `branchops-cannabis-policy-interpretation-review`
1. Test branchops-cannabis-policy-interpretation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-policy-interpretation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-policy-interpretation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 237. `branchops-cannabis-license-application-package`
1. Test branchops-cannabis-license-application-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-license-application-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-license-application-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 238. `branchops-cannabis-conditional-license-readiness`
1. Test branchops-cannabis-conditional-license-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-conditional-license-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-conditional-license-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 239. `branchops-cannabis-annual-renewal-checklist`
1. Test branchops-cannabis-annual-renewal-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-annual-renewal-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-annual-renewal-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 240. `branchops-cannabis-license-class-routing`
1. Test branchops-cannabis-license-class-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-license-class-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-license-class-routing: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 241. `branchops-cannabis-application-evidence-checklist`
1. Test branchops-cannabis-application-evidence-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-application-evidence-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-application-evidence-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 242. `branchops-cannabis-microbusiness-review`
1. Test branchops-cannabis-microbusiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-microbusiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-microbusiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 243. `branchops-cannabis-priority-application-review`
1. Test branchops-cannabis-priority-application-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-priority-application-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-priority-application-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 244. `branchops-cannabis-license-fee-deadline-tracker`
1. Test branchops-cannabis-license-fee-deadline-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-license-fee-deadline-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-license-fee-deadline-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 245. `branchops-cannabis-licensed-counterparty-verification`
1. Test branchops-cannabis-licensed-counterparty-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-licensed-counterparty-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-licensed-counterparty-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 246. `branchops-cannabis-license-audit-readiness`
1. Test branchops-cannabis-license-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-license-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-license-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 247. `branchops-cannabis-municipal-zoning-readiness`
1. Test branchops-cannabis-municipal-zoning-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-municipal-zoning-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-municipal-zoning-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 248. `branchops-cannabis-site-control-document-review`
1. Test branchops-cannabis-site-control-document-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-site-control-document-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-site-control-document-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 249. `branchops-cannabis-facility-readiness-review`
1. Test branchops-cannabis-facility-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-facility-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-facility-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 250. `branchops-cannabis-security-plan-outline`
1. Test branchops-cannabis-security-plan-outline: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-security-plan-outline: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-security-plan-outline: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 251. `branchops-cannabis-surveillance-access-review`
1. Test branchops-cannabis-surveillance-access-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-surveillance-access-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-surveillance-access-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 252. `branchops-cannabis-real-estate-risk-review`
1. Test branchops-cannabis-real-estate-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-real-estate-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-real-estate-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 253. `branchops-cannabis-host-community-package`
1. Test branchops-cannabis-host-community-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-host-community-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-host-community-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 254. `branchops-cannabis-local-approval-checklist`
1. Test branchops-cannabis-local-approval-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-local-approval-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-local-approval-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 255. `branchops-cannabis-facility-buildout-readiness`
1. Test branchops-cannabis-facility-buildout-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-facility-buildout-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-facility-buildout-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 256. `branchops-cannabis-insurance-requirements-review`
1. Test branchops-cannabis-insurance-requirements-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-insurance-requirements-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-insurance-requirements-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 257. `branchops-cannabis-cultivation-compliance-review`
1. Test branchops-cannabis-cultivation-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-cultivation-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-cultivation-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 258. `branchops-cannabis-plant-tagging-inventory-review`
1. Test branchops-cannabis-plant-tagging-inventory-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-plant-tagging-inventory-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-plant-tagging-inventory-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 259. `branchops-cannabis-harvest-batch-documentation`
1. Test branchops-cannabis-harvest-batch-documentation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-harvest-batch-documentation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-harvest-batch-documentation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 260. `branchops-cannabis-pesticide-input-compliance-review`
1. Test branchops-cannabis-pesticide-input-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-pesticide-input-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-pesticide-input-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 261. `branchops-cannabis-environmental-controls-compliance`
1. Test branchops-cannabis-environmental-controls-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-environmental-controls-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-environmental-controls-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 262. `branchops-hemp-license-readiness-review`
1. Test branchops-hemp-license-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-hemp-license-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-hemp-license-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 263. `branchops-hemp-pre-harvest-sampling-checklist`
1. Test branchops-hemp-pre-harvest-sampling-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-hemp-pre-harvest-sampling-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-hemp-pre-harvest-sampling-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 264. `branchops-hemp-thc-testing-compliance-review`
1. Test branchops-hemp-thc-testing-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-hemp-thc-testing-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-hemp-thc-testing-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 265. `branchops-hemp-disposal-remediation-records`
1. Test branchops-hemp-disposal-remediation-records: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-hemp-disposal-remediation-records: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-hemp-disposal-remediation-records: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 266. `branchops-hemp-crop-reporting-workflow`
1. Test branchops-hemp-crop-reporting-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-hemp-crop-reporting-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-hemp-crop-reporting-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 267. `branchops-cannabis-manufacturing-compliance-review`
1. Test branchops-cannabis-manufacturing-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-manufacturing-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-manufacturing-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 268. `branchops-cannabis-batch-record-review`
1. Test branchops-cannabis-batch-record-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-batch-record-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-batch-record-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 269. `branchops-cannabis-processing-sop-review`
1. Test branchops-cannabis-processing-sop-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-processing-sop-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-processing-sop-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 270. `branchops-cannabis-gmp-sanitation-review`
1. Test branchops-cannabis-gmp-sanitation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-gmp-sanitation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-gmp-sanitation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 271. `branchops-cannabis-ingredient-input-control-review`
1. Test branchops-cannabis-ingredient-input-control-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-ingredient-input-control-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-ingredient-input-control-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 272. `branchops-cannabis-extraction-compliance-review`
1. Test branchops-cannabis-extraction-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-extraction-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-extraction-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 273. `branchops-cannabis-extraction-safety-risk-review`
1. Test branchops-cannabis-extraction-safety-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-extraction-safety-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-extraction-safety-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 274. `branchops-cannabis-concentrate-batch-documentation`
1. Test branchops-cannabis-concentrate-batch-documentation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-concentrate-batch-documentation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-concentrate-batch-documentation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 275. `branchops-cannabis-equipment-compliance-review`
1. Test branchops-cannabis-equipment-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-equipment-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-equipment-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 276. `branchops-cannabis-manufacturing-audit-readiness`
1. Test branchops-cannabis-manufacturing-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-manufacturing-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-manufacturing-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 277. `branchops-cannabis-edibles-compliance-review`
1. Test branchops-cannabis-edibles-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-edibles-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-edibles-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 278. `branchops-cannabis-beverage-compliance-review`
1. Test branchops-cannabis-beverage-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-beverage-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-beverage-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 279. `branchops-cannabis-topicals-compliance-review`
1. Test branchops-cannabis-topicals-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-topicals-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-topicals-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 280. `branchops-cannabis-infused-product-spec-review`
1. Test branchops-cannabis-infused-product-spec-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-infused-product-spec-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-infused-product-spec-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 281. `branchops-cannabis-serving-package-limit-review`
1. Test branchops-cannabis-serving-package-limit-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-serving-package-limit-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-serving-package-limit-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 282. `branchops-cannabis-allergen-ingredient-review`
1. Test branchops-cannabis-allergen-ingredient-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-allergen-ingredient-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-allergen-ingredient-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 283. `branchops-cannabis-food-safety-review`
1. Test branchops-cannabis-food-safety-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-food-safety-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-food-safety-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 284. `branchops-cannabis-product-stability-review`
1. Test branchops-cannabis-product-stability-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-product-stability-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-product-stability-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 285. `branchops-cannabis-product-safety-review`
1. Test branchops-cannabis-product-safety-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-product-safety-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-product-safety-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 286. `branchops-cannabis-product-launch-gate`
1. Test branchops-cannabis-product-launch-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-product-launch-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-product-launch-gate: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 287. `branchops-cannabis-testing-coa-review`
1. Test branchops-cannabis-testing-coa-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-testing-coa-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-testing-coa-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 288. `branchops-cannabis-potency-test-review`
1. Test branchops-cannabis-potency-test-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-potency-test-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-potency-test-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 289. `branchops-cannabis-contaminant-test-review`
1. Test branchops-cannabis-contaminant-test-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-contaminant-test-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-contaminant-test-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 290. `branchops-cannabis-lab-vendor-due-diligence`
1. Test branchops-cannabis-lab-vendor-due-diligence: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-lab-vendor-due-diligence: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-lab-vendor-due-diligence: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 291. `branchops-cannabis-failed-test-escalation`
1. Test branchops-cannabis-failed-test-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-failed-test-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-failed-test-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 292. `branchops-cannabis-batch-release-review`
1. Test branchops-cannabis-batch-release-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-batch-release-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-batch-release-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 293. `branchops-cannabis-deviation-capa-workflow`
1. Test branchops-cannabis-deviation-capa-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-deviation-capa-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-deviation-capa-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 294. `branchops-cannabis-qa-audit-readiness`
1. Test branchops-cannabis-qa-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-qa-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-qa-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 295. `branchops-cannabis-recall-readiness-review`
1. Test branchops-cannabis-recall-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-recall-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-recall-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 296. `branchops-cannabis-adverse-event-triage`
1. Test branchops-cannabis-adverse-event-triage: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-adverse-event-triage: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-adverse-event-triage: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 297. `branchops-cannabis-packaging-labeling-review`
1. Test branchops-cannabis-packaging-labeling-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-packaging-labeling-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-packaging-labeling-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 298. `branchops-cannabis-warning-label-review`
1. Test branchops-cannabis-warning-label-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-warning-label-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-warning-label-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 299. `branchops-cannabis-child-resistant-packaging-review`
1. Test branchops-cannabis-child-resistant-packaging-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-child-resistant-packaging-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-child-resistant-packaging-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 300. `branchops-cannabis-universal-symbol-review`
1. Test branchops-cannabis-universal-symbol-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-universal-symbol-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-universal-symbol-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 301. `branchops-cannabis-label-artwork-review`
1. Test branchops-cannabis-label-artwork-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-label-artwork-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-label-artwork-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 302. `branchops-cannabis-health-claims-review`
1. Test branchops-cannabis-health-claims-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-health-claims-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-health-claims-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 303. `branchops-cannabis-advertising-restriction-review`
1. Test branchops-cannabis-advertising-restriction-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-advertising-restriction-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-advertising-restriction-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 304. `branchops-cannabis-age-gated-marketing-review`
1. Test branchops-cannabis-age-gated-marketing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-age-gated-marketing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-age-gated-marketing-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 305. `branchops-cannabis-social-media-compliance-review`
1. Test branchops-cannabis-social-media-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-social-media-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-social-media-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 306. `branchops-cannabis-public-claims-review`
1. Test branchops-cannabis-public-claims-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-public-claims-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-public-claims-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 307. `branchops-cannabis-dispensary-operations-review`
1. Test branchops-cannabis-dispensary-operations-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-dispensary-operations-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-dispensary-operations-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 308. `branchops-cannabis-age-verification-sop`
1. Test branchops-cannabis-age-verification-sop: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-age-verification-sop: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-age-verification-sop: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 309. `branchops-cannabis-pos-compliance-review`
1. Test branchops-cannabis-pos-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-pos-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-pos-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 310. `branchops-cannabis-purchase-limit-review`
1. Test branchops-cannabis-purchase-limit-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-purchase-limit-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-purchase-limit-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 311. `branchops-cannabis-budtender-training-package`
1. Test branchops-cannabis-budtender-training-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-budtender-training-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-budtender-training-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 312. `branchops-cannabis-delivery-compliance-review`
1. Test branchops-cannabis-delivery-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-delivery-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-delivery-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 313. `branchops-cannabis-transport-manifest-review`
1. Test branchops-cannabis-transport-manifest-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-transport-manifest-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-transport-manifest-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 314. `branchops-cannabis-chain-of-custody-review`
1. Test branchops-cannabis-chain-of-custody-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-chain-of-custody-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-chain-of-custody-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 315. `branchops-cannabis-wholesale-channel-review`
1. Test branchops-cannabis-wholesale-channel-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-wholesale-channel-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-wholesale-channel-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 316. `branchops-cannabis-b2b-license-verification`
1. Test branchops-cannabis-b2b-license-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-b2b-license-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-b2b-license-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 317. `branchops-cannabis-seed-to-sale-system-design`
1. Test branchops-cannabis-seed-to-sale-system-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-seed-to-sale-system-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-seed-to-sale-system-design: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 318. `branchops-cannabis-metrc-track-trace-review`
1. Test branchops-cannabis-metrc-track-trace-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-metrc-track-trace-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-metrc-track-trace-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 319. `branchops-cannabis-inventory-reconciliation`
1. Test branchops-cannabis-inventory-reconciliation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-inventory-reconciliation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-inventory-reconciliation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 320. `branchops-cannabis-waste-log-review`
1. Test branchops-cannabis-waste-log-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-waste-log-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-waste-log-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 321. `branchops-cannabis-transfer-manifest-review`
1. Test branchops-cannabis-transfer-manifest-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-transfer-manifest-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-transfer-manifest-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 322. `branchops-cannabis-inventory-audit-readiness`
1. Test branchops-cannabis-inventory-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-inventory-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-inventory-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 323. `branchops-cannabis-crm-privacy-review`
1. Test branchops-cannabis-crm-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-crm-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-crm-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 324. `branchops-cannabis-patient-data-privacy-review`
1. Test branchops-cannabis-patient-data-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-patient-data-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-patient-data-privacy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 325. `branchops-cannabis-retention-policy-review`
1. Test branchops-cannabis-retention-policy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-retention-policy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-retention-policy-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 326. `branchops-cannabis-source-of-truth-schema`
1. Test branchops-cannabis-source-of-truth-schema: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-source-of-truth-schema: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-source-of-truth-schema: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 327. `branchops-cannabis-banking-readiness-review`
1. Test branchops-cannabis-banking-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-banking-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-banking-readiness-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 328. `branchops-cannabis-280e-tax-risk-review`
1. Test branchops-cannabis-280e-tax-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-280e-tax-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-280e-tax-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 329. `branchops-cannabis-cash-handling-controls`
1. Test branchops-cannabis-cash-handling-controls: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-cash-handling-controls: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-cash-handling-controls: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 330. `branchops-cannabis-payment-system-risk-review`
1. Test branchops-cannabis-payment-system-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-payment-system-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-payment-system-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 331. `branchops-cannabis-excise-sales-tax-tracker`
1. Test branchops-cannabis-excise-sales-tax-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-excise-sales-tax-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-excise-sales-tax-tracker: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 332. `branchops-cannabis-bookkeeping-workflow`
1. Test branchops-cannabis-bookkeeping-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-bookkeeping-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-bookkeeping-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 333. `branchops-cannabis-monthly-close-checklist`
1. Test branchops-cannabis-monthly-close-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-monthly-close-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-monthly-close-checklist: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 334. `branchops-cannabis-cpa-handoff-package`
1. Test branchops-cannabis-cpa-handoff-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-cpa-handoff-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-cpa-handoff-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 335. `branchops-cannabis-financial-risk-escalation`
1. Test branchops-cannabis-financial-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-financial-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-financial-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 336. `branchops-cannabis-investor-diligence-package`
1. Test branchops-cannabis-investor-diligence-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-investor-diligence-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-investor-diligence-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 337. `branchops-cannabis-medical-program-compliance`
1. Test branchops-cannabis-medical-program-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-medical-program-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-medical-program-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 338. `branchops-cannabis-patient-access-workflow`
1. Test branchops-cannabis-patient-access-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-patient-access-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-patient-access-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 339. `branchops-cannabis-responsible-use-education`
1. Test branchops-cannabis-responsible-use-education: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-responsible-use-education: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-responsible-use-education: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 340. `branchops-cannabis-adult-use-consumer-compliance`
1. Test branchops-cannabis-adult-use-consumer-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-adult-use-consumer-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-adult-use-consumer-compliance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 341. `branchops-cannabis-workforce-training-program`
1. Test branchops-cannabis-workforce-training-program: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-workforce-training-program: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-workforce-training-program: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 342. `branchops-cannabis-sop-certification-pathway`
1. Test branchops-cannabis-sop-certification-pathway: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-sop-certification-pathway: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-sop-certification-pathway: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 343. `branchops-cannabis-school-partnership-review`
1. Test branchops-cannabis-school-partnership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-school-partnership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-school-partnership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 344. `branchops-cannabis-community-education-package`
1. Test branchops-cannabis-community-education-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-community-education-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-community-education-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 345. `branchops-cannabis-staff-training-records`
1. Test branchops-cannabis-staff-training-records: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-staff-training-records: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-staff-training-records: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 346. `branchops-cannabis-training-audit-readiness`
1. Test branchops-cannabis-training-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-training-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-training-audit-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 347. `branchops-cannabis-social-equity-eligibility-review`
1. Test branchops-cannabis-social-equity-eligibility-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-social-equity-eligibility-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-social-equity-eligibility-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 348. `branchops-cannabis-impact-zone-review`
1. Test branchops-cannabis-impact-zone-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-impact-zone-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-impact-zone-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 349. `branchops-cannabis-community-benefits-plan`
1. Test branchops-cannabis-community-benefits-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-community-benefits-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-community-benefits-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 350. `branchops-cannabis-diversity-documentation-review`
1. Test branchops-cannabis-diversity-documentation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-diversity-documentation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-diversity-documentation-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 351. `branchops-cannabis-local-hiring-plan`
1. Test branchops-cannabis-local-hiring-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-local-hiring-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-local-hiring-plan: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 352. `branchops-cannabis-grant-opportunity-review`
1. Test branchops-cannabis-grant-opportunity-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-grant-opportunity-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-grant-opportunity-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 353. `branchops-cannabis-economic-development-package`
1. Test branchops-cannabis-economic-development-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-economic-development-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-economic-development-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 354. `branchops-cannabis-public-sector-compliance-review`
1. Test branchops-cannabis-public-sector-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-public-sector-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-public-sector-compliance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 355. `branchops-cannabis-application-submission-readiness`
1. Test branchops-cannabis-application-submission-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-application-submission-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-application-submission-readiness: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 356. `branchops-cannabis-community-impact-report`
1. Test branchops-cannabis-community-impact-report: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-community-impact-report: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-community-impact-report: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 357. `branchops-cannabis-ip-ownership-review`
1. Test branchops-cannabis-ip-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-ip-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-ip-ownership-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 358. `branchops-cannabis-brand-usage-review`
1. Test branchops-cannabis-brand-usage-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-brand-usage-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-brand-usage-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 359. `branchops-cannabis-licensing-package-builder`
1. Test branchops-cannabis-licensing-package-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-licensing-package-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-licensing-package-builder: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 360. `branchops-cannabis-white-label-product-package`
1. Test branchops-cannabis-white-label-product-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-white-label-product-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-white-label-product-package: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 361. `branchops-cannabis-royalty-model-review`
1. Test branchops-cannabis-royalty-model-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-royalty-model-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-royalty-model-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 362. `branchops-cannabis-brand-risk-escalation`
1. Test branchops-cannabis-brand-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-brand-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-brand-risk-escalation: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 363. `branchops-cannabis-product-line-architecture`
1. Test branchops-cannabis-product-line-architecture: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-product-line-architecture: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-product-line-architecture: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 364. `branchops-cannabis-menu-engineering-review`
1. Test branchops-cannabis-menu-engineering-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-menu-engineering-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-menu-engineering-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 365. `branchops-cannabis-media-content-clearance`
1. Test branchops-cannabis-media-content-clearance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-media-content-clearance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-media-content-clearance: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 366. `branchops-cannabis-licensee-onboarding-workflow`
1. Test branchops-cannabis-licensee-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-licensee-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-licensee-onboarding-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 367. `branchops-cannabis-vendor-due-diligence`
1. Test branchops-cannabis-vendor-due-diligence: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-vendor-due-diligence: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-vendor-due-diligence: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 368. `branchops-cannabis-supplier-license-verification`
1. Test branchops-cannabis-supplier-license-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-supplier-license-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-supplier-license-verification: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 369. `branchops-cannabis-white-label-partner-review`
1. Test branchops-cannabis-white-label-partner-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-white-label-partner-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-white-label-partner-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 370. `branchops-cannabis-vendor-insurance-review`
1. Test branchops-cannabis-vendor-insurance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-vendor-insurance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-vendor-insurance-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 371. `branchops-cannabis-recall-workflow`
1. Test branchops-cannabis-recall-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-recall-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-recall-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 372. `branchops-cannabis-product-hold-quarantine-workflow`
1. Test branchops-cannabis-product-hold-quarantine-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-product-hold-quarantine-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-product-hold-quarantine-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 373. `branchops-cannabis-incident-report-workflow`
1. Test branchops-cannabis-incident-report-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-incident-report-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-incident-report-workflow: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 374. `branchops-cannabis-regulatory-notification-handoff`
1. Test branchops-cannabis-regulatory-notification-handoff: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-regulatory-notification-handoff: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-regulatory-notification-handoff: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 375. `branchops-cannabis-insurance-risk-review`
1. Test branchops-cannabis-insurance-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-insurance-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-insurance-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.

## 376. `branchops-cannabis-product-liability-risk-review`
1. Test branchops-cannabis-product-liability-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Normal case: all required context is available and requested output is a draft/readiness artifact.
2. Test branchops-cannabis-product-liability-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Edge case: jurisdiction/source-of-truth or reviewer ownership is ambiguous and must be clarified or escalated.
3. Test branchops-cannabis-product-liability-risk-review: route a realistic request, select department and role card, select skill pack, check permission boundaries, identify reviewer chain, validation, source-of-truth updates, decision, and founder gate if required. Failure/escalation case: the request asks for a blocked action or unsupported claim; return BLOCK or ESCALATE with recovery steps.
