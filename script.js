const menuToggle=document.getElementById("menu-toggle");const navLinks=document.getElementById("nav-links");const contactForm=document.getElementById("contact-form");const formStatus=document.getElementById("form-status");const waitlistForm=document.getElementById("waitlist-form");const waitlistStatus=document.getElementById("waitlist-status");const prefersReducedMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const publicIntakePath="/api/public/intake/contact";function resolvePublicIntakeEndpoint(){const explicitBaseUrl=document.querySelector('meta[name="branchops-public-api-base"]')?.getAttribute('content')?.trim();if(explicitBaseUrl){return`${explicitBaseUrl.replace(/\/$/,"")}${publicIntakePath}`}const host=window.location.hostname;if(host==='localhost'||host==='127.0.0.1'){return`http://localhost:4000${publicIntakePath}`}return`${window.location.origin}${publicIntakePath}`}function ensureHoneypotField(form,id){if(form.querySelector('input[name="website"]')){return}const wrapper=document.createElement('div');wrapper.setAttribute('aria-hidden','true');wrapper.style.position='absolute';wrapper.style.left='-9999px';wrapper.style.width='1px';wrapper.style.height='1px';wrapper.style.overflow='hidden';const label=document.createElement('label');label.textContent='Leave this field empty';label.htmlFor=id;const input=document.createElement('input');input.type='text';input.name='website';input.id=id;input.tabIndex=-1;input.autocomplete='off';wrapper.append(label,input);form.prepend(wrapper)}async function submitInquiry({payload,form,statusNode,submitLabel}){const submitButton=form.querySelector('button[type="submit"]');const previousButtonLabel=submitButton?.textContent??submitLabel;statusNode.textContent='Sending your inquiry…';if(submitButton){submitButton.disabled=true;submitButton.textContent='Sending…'}try{const response=await fetch(resolvePublicIntakeEndpoint(),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const responseBody=await response.json().catch(()=>null);if(!response.ok){throw new Error(responseBody?.message||'The inquiry could not be sent right now. Please try again shortly.')}form.reset();ensureHoneypotField(form,form===waitlistForm?'trapcotton-waitlist-website':'trapcotton-contact-website');statusNode.textContent='Your submission was received. The team will follow up through your provided email.'}catch(error){statusNode.textContent=error instanceof Error?error.message:'The inquiry could not be sent right now. Please try again shortly.'}finally{if(submitButton){submitButton.disabled=false;submitButton.textContent=previousButtonLabel}}}if(navLinks){navLinks.dataset.open='false'}if(menuToggle&&navLinks){menuToggle.addEventListener('click',()=>{const isOpen=navLinks.dataset.open==='true';navLinks.dataset.open=String(!isOpen);menuToggle.setAttribute('aria-expanded',String(!isOpen))});navLinks.querySelectorAll('a').forEach(link=>{link.addEventListener('click',()=>{navLinks.dataset.open='false';menuToggle.setAttribute('aria-expanded','false')})})}if(!prefersReducedMotion){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.18});document.querySelectorAll('.reveal').forEach(section=>observer.observe(section))}else{document.querySelectorAll('.reveal').forEach(section=>section.classList.add('visible'))}if(waitlistForm&&waitlistStatus){ensureHoneypotField(waitlistForm,'trapcotton-waitlist-website');waitlistForm.addEventListener('submit',async event=>{event.preventDefault();const formData=new FormData(waitlistForm);const cityInterest=String(formData.get('cityInterest')??'').trim();const message=cityInterest?`Please add me to the TrapCotton waitlist. City interest: ${cityInterest}.`:'Please add me to the TrapCotton waitlist for future drop updates.';await submitInquiry({payload:{sourceSite:'off-da-branch-site',inquiryType:'waitlist',name:String(formData.get('name')??'').trim(),email:String(formData.get('email')??'').trim(),companyName:null,phone:null,message,website:String(formData.get('website')??'').trim()},form:waitlistForm,statusNode:waitlistStatus,submitLabel:'Join waitlist'})})}if(contactForm&&formStatus){ensureHoneypotField(contactForm,'trapcotton-contact-website');contactForm.addEventListener('submit',async event=>{event.preventDefault();const formData=new FormData(contactForm);await submitInquiry({payload:{sourceSite:'off-da-branch-site',inquiryType:String(formData.get('inquiryType')??'general').trim()||'general',name:String(formData.get('name')??'').trim(),email:String(formData.get('email')??'').trim(),companyName:String(formData.get('companyName')??'').trim()||null,phone:String(formData.get('phone')??'').trim()||null,message:String(formData.get('message')??'').trim(),website:String(formData.get('website')??'').trim()},form:contactForm,statusNode:formStatus,submitLabel:'Send inquiry'})})}